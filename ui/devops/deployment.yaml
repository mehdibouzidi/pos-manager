apiVersion: apps/v1
kind: Deployment
metadata:
  name: pos-ui-deployment
  namespace: app-dev
  labels:
    app: pos-ui
spec:
  replicas: 1
  selector:
    matchLabels:
      app: pos-ui
  template:
    metadata:
      labels:
        app: pos-ui
    spec:
      imagePullSecrets:
      - name: ghcr-secret
      containers:
      - name: pos-ui
        image: ghcr.io/GITHUB_OWNER/pos-manager-ui:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 80
        resources:
          limits:
            cpu: 50m
            memory: 64Mi
          requests:
            cpu: 50m
            memory: 64Mi
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 25%
      maxSurge: 25%
