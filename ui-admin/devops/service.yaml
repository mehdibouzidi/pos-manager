apiVersion: v1
kind: Service
metadata:
  name: pos-admin-service
  namespace: app-dev
spec:
  selector:
    app: pos-admin
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
  type: ClusterIP
